<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="4dp"
        android:minHeight="?attr/actionBarSize"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintVertical_bias="0.0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        >

        <ImageView
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/gobackarrow" />

        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchView_fragment_myroutes"
            android:layout_width="250dp"
            android:layout_height="30dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/custom_searchview_myroutes"
            android:focusable="true"
            android:nextFocusLeft="@id/searchView_fragment_myroutes"
            android:nextFocusUp="@id/searchView_fragment_myroutes"
            app:iconifiedByDefault="true"
            app:queryBackground="@android:color/transparent"
            app:queryHint="Ara"
            app:searchIcon="@drawable/searchview_icon"
            />

    </androidx.appcompat.widget.Toolbar>

    <Button
        android:id="@+id/yeniRotaOlustur_fragment_myroutes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/custom_rectangle_design"
        android:drawableEnd="@mipmap/createnewroute_icon"
        android:elevation="10dp"
        android:minWidth="200dp"
        android:minHeight="60dp"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:text="@string/yeniRotaOlustur_activityMyroutes"
        android:textAlignment="textStart"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.109" />

</androidx.constraintlayout.widget.ConstraintLayout>